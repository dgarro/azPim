"use strict"

/**
 * Allows for custom filtering of groups
 * Default is to return all availale groups
 */
const GroupFilter = (function () {
    return {
        filter: (data) => (true)
    };
})();